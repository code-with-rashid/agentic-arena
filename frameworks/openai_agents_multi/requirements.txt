# Same library, different wiring: openai_agents_multi is the handoff-chain
# contrast entry for the single-agent `openai_agents` adapter. It deliberately
# does NOT carry its own pins - a second copy of the same versions would drift
# from the first, and then the two entries would no longer be comparable, which
# is the only reason this entry exists.
-r ../openai_agents/requirements.txt
