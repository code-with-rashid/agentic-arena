# Pinned so scorecards are reproducible. Bump deliberately, then refresh results/.
#
# Only the `-openai` client package is needed (it pulls `agent-framework-core`).
# The `agent-framework` meta-package instead pulls `agent-framework-core[all]`
# (azure, boto3, redis, qdrant, ollama, numpy, ...), none of which this adapter
# uses.
agent-framework-core==1.16.0
agent-framework-openai==1.14.1
